import { SearchhelperPipe } from './searchhelper.pipe';

describe('SearchhelperPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchhelperPipe();
    expect(pipe).toBeTruthy();
  });
});
